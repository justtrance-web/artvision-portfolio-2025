---
name: tilda-to-html
description: >
  Перенос сайтов Tilda в автономные HTML-файлы для последующей натяжки на WordPress или другие CMS.
  Триггеры: перенеси тильду, скачай сайт tilda, сделай автономную версию, натяжка тильды на wordpress, html из tilda, экспорт tilda.
  ТАКЖЕ АКТИВИРУЕТСЯ: если пользователь отправляет ссылку вида *.tilda.ws или *.tilda.cc
  Создаёт полностью автономный пакет со всеми ресурсами (CSS, JS, изображения, шрифты), готовый к интеграции с любой CMS.
---

# Tilda to HTML — Полный перенос сайта

## 🚨 АВТОМАТИЧЕСКИЙ РЕЖИМ

**Если пользователь скинул ссылку на Tilda (*.tilda.ws, *.tilda.cc) — сразу запроси код страницы:**

### НЕМЕДЛЕННО ОТВЕТЬ:

```
Отлично! Чтобы создать автономную версию, мне нужен исходный код страницы.

**Как получить:**
1. Открой страницу в браузере
2. Нажми **Ctrl+U** (Windows/Linux) или **Cmd+Option+U** (Mac)
3. Скопируй ВЕСЬ код (Ctrl+A → Ctrl+C)
4. Вставь сюда

Или: правый клик → "Посмотреть код страницы" → скопировать всё

После этого я:
✅ Извлеку все стили и скрипты
✅ Создам список ресурсов для скачивания  
✅ Сделаю автономный HTML с локальными путями
✅ Дам готовый пакет для WordPress
```

---

## После получения кода страницы — WORKFLOW:

### Шаг 1: Сохранить HTML
```python
# Сохранить полученный код как HTML-файл
with open('page.html', 'w', encoding='utf-8') as f:
    f.write(user_code)
```

### Шаг 2: Извлечь все ресурсы
```python
import re

html = user_code

# CSS файлы
css_urls = re.findall(r'href="([^"]*\.css[^"]*)"', html)
css_urls += re.findall(r"href='([^']*\.css[^']*)'", html)

# JS файлы
js_urls = re.findall(r'src="([^"]*\.js[^"]*)"', html)
js_urls += re.findall(r"src='([^']*\.js[^']*)'", html)

# Изображения
img_urls = re.findall(r'(?:src|data-original|data-src)="([^"]*\.(?:jpg|jpeg|png|gif|webp|svg)[^"]*)"', html)

# Шрифты
font_urls = re.findall(r'url\(["\']?([^"\')\s]*\.(?:woff2?|ttf|eot)[^"\')\s]*)["\']?\)', html)
```

### Шаг 3: Создать скрипт скачивания для пользователя
```bash
#!/bin/bash
# download_assets.sh — запустить на своём компьютере

mkdir -p tilda_assets/{css,js,images,fonts}

# CSS
curl -o "tilda_assets/css/file.css" "https://static.tildacdn.com/..."

# JS  
curl -o "tilda_assets/js/file.js" "https://static.tildacdn.com/..."

# Изображения
curl -o "tilda_assets/images/img.jpg" "https://thumb.tildacdn.com/..."
```

### Шаг 4: Заменить пути в HTML
```python
replacements = [
    (r'https://static\.tildacdn\.com/[^/]*/css/', 'tilda_assets/css/'),
    (r'https://static\.tildacdn\.com/[^/]*/js/', 'tilda_assets/js/'),
    (r'https://static\.tildacdn\.com/', 'tilda_assets/'),
    (r'https://thumb\.tildacdn\.com/[^"]*/', 'tilda_assets/images/'),
    (r'https://tilda\.ws/', ''),
]

for pattern, replacement in replacements:
    html = re.sub(pattern, replacement, html)
```

### Шаг 5: Удалить Tilda-специфичное
```python
# Удалить аналитику
html = re.sub(r'<script[^>]*tilda-stat[^>]*>.*?</script>', '', html, flags=re.DOTALL)

# Удалить внешние формы
html = re.sub(r'action="https://forms\.tildacdn\.com[^"]*"', 'action="#"', html)
```

### Шаг 6: Выдать результат
1. **Готовый HTML-файл** с локальными путями
2. **Скрипт скачивания ресурсов** (bash/PowerShell)
3. **Инструкцию** по запуску

---

## ИТОГОВЫЙ ПАКЕТ для пользователя:

```
site_package/
├── index.html                    # Готовый HTML с локальными путями
├── download_assets.sh            # Скрипт скачивания (Mac/Linux)
├── download_assets.ps1           # Скрипт скачивания (Windows)
├── tilda_assets/                 # Папка для ресурсов (пустая)
│   ├── css/
│   ├── js/
│   ├── images/
│   └── fonts/
├── SETUP.sh                      # Установка после скачивания
└── README.txt                    # Инструкция
```

**Инструкция для пользователя:**
1. Распаковать архив
2. Запустить `download_assets.sh` (или .ps1 для Windows)
3. Подождать пока скачаются все файлы
4. Открыть `index.html` — сайт работает офлайн!

---

## Quick Start (5 минут)

```bash
# 1. Скачать сайт
./scripts/download_tilda.sh https://site.tilda.ws output_folder "page1 page2"

# 2. Умный автофикс путей
cd output_folder
../scripts/smart_fix.sh

# 3. Проверить в браузере
open index.html
```

## Обзор процесса

1. **Анализ** — определить все страницы и ресурсы сайта
2. **Скачивание** — загрузить HTML + все зависимости
3. **Исправление путей** — привести все пути к относительным
4. **Диагностика** — проверить соответствие имён файлов
5. **Тестирование** — убедиться в корректной работе офлайн
6. **Упаковка** — создать архив с установочными скриптами

## Критические нюансы Tilda

### Проблема именования файлов

Tilda генерирует уникальные имена CSS/JS для каждой страницы:
- `tilda-blocks-page51328759.min.css` — для главной
- `tilda-blocks-page60669247.min.css` — для внутренней страницы

**Важно:** При скачивании wget может создавать файлы с generic-именами, а HTML ссылается на page-specific имена.

### Паттерны несоответствия имён

| HTML ожидает | Скачанный файл |
|--------------|----------------|
| `main-tilda-blocks-page51328759.min.css` | `main-blocks.min.css` |
| `all-on-4-tilda-blocks-page60669247.min.css` | `tilda-blocks-page60669247.min.css` |
| `main-tilda-scripts-page51328759.min.js` | `main-scripts.min.js` |

### Решение: скрипт диагностики

После скачивания запустить `scripts/diagnose_assets.sh` для автоматического выявления несоответствий.

## Структура автономного пакета

```
site_package/
├── index.html                    # Главная страница
├── [page-name].html              # Внутренние страницы
├── tilda_assets/
│   ├── css/                      # Все стили
│   ├── js/                       # Все скрипты
│   ├── images/                   # Изображения
│   └── fonts/                    # Шрифты
├── scripts/
│   ├── SETUP.sh                  # Установка для Mac/Linux
│   └── SETUP.ps1                 # Установка для Windows
└── README.txt                    # Инструкция для клиента
```

## Workflow: Создание пакета

### Шаг 1: Анализ сайта

```bash
# Получить список всех страниц
curl -s "https://site.tilda.ws/sitemap.xml" | grep -oP '(?<=<loc>)[^<]+'

# Или вручную собрать URL всех страниц
```

### Шаг 2: Скачивание ресурсов

Использовать `scripts/download_tilda.sh` с параметрами:
- `BASE_URL` — домен сайта
- `PAGES` — список страниц через пробел
- `OUTPUT_DIR` — папка для сохранения

### Шаг 3: Исправление путей в HTML

Заменить абсолютные пути на относительные:
```
https://static.tildacdn.com/... → tilda_assets/...
https://site.tilda.ws/... → ./...
```

### Шаг 4: Диагностика и исправление имён

Запустить `scripts/diagnose_assets.sh` — скрипт:
1. Извлекает все пути CSS/JS из HTML-файлов
2. Проверяет существование файлов
3. Генерирует команды переименования

### Шаг 5: Тестирование

```bash
# Открыть в браузере без сервера
open index.html

# Или запустить локальный сервер
python3 -m http.server 8000
```

### Шаг 6: Упаковка

```bash
zip -r site_package.zip site_package/
```

## Подготовка к WordPress

### Что нужно для натяжки

1. **Чистый HTML** — без Tilda-специфичных скриптов
2. **Локальные ресурсы** — все CSS/JS/изображения в папке темы
3. **Относительные пути** — для переносимости

### Рекомендуемая структура темы WP

```
theme-name/
├── style.css                     # Главный стиль темы
├── index.php                     # Главный шаблон
├── page-*.php                    # Шаблоны страниц
├── header.php                    # Шапка
├── footer.php                    # Подвал
└── assets/
    ├── css/                      # CSS из Tilda
    ├── js/                       # JS из Tilda
    └── images/                   # Изображения
```

### Удалить перед натяжкой

- Tilda-аналитику (`tilda-stat.js`)
- Tilda-формы (заменить на WP-формы)
- Внешние зависимости от `tildacdn.com`

## Типичные проблемы и решения

### Проблема: Страница без стилей

**Причина:** Несоответствие имён CSS-файлов
**Диагностика:**
```bash
grep -o 'href="[^"]*css[^"]*"' page.html
ls tilda_assets/css/
```
**Решение:** Переименовать или скопировать файл

### Проблема: Битые изображения

**Причина:** Абсолютные пути к CDN
**Решение:** Найти и заменить:
```bash
sed -i 's|https://static.tildacdn.com/|tilda_assets/|g' *.html
```

### Проблема: Не работают формы

**Причина:** Tilda-формы требуют API
**Решение:** Заменить на статичные формы или WP-плагины

### Проблема: CORS при локальном открытии

**Причина:** Браузер блокирует локальные запросы
**Решение:** Использовать локальный сервер или флаг `--allow-file-access-from-files`

## См. также

- `scripts/download_tilda.sh` — скрипт скачивания
- `scripts/diagnose_assets.sh` — базовая диагностика путей
- `scripts/smart_fix.sh` — умная диагностика с автофиксом (рекомендуется)
- `scripts/fix_paths.py` — Python-скрипт автоисправления
- `references/tilda-cdn-mapping.md` — карта CDN-путей Tilda
- `references/wordpress-integration.md` — гайд по натяжке на WordPress
- `references/real-cases.md` — реальные кейсы и решения
- `assets/SETUP.sh` — шаблон установщика для Mac/Linux
- `assets/SETUP.ps1` — шаблон установщика для Windows
- `assets/README_TEMPLATE.txt` — шаблон README для клиента
